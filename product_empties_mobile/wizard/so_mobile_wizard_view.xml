<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sale_order_mobile_wizard_form" model="ir.ui.view">
        <field name="name">Create/Open Sale Order</field>
        <field name="model">so.mobile.wizard</field>
        <field name="arch" type="xml">
            <form string="Invoice Sales Order">
                <group>
                    <field name="so_id"/>
                </group>
                <footer>
                    <button name="open_so_mobile_old" string="Open" type="object"
                        context="{'open_invoices': True}" class="btn-primary"/>
                    <button name="open_so_mobile_new" string="Create New" type="object"
                        class="btn-primary"/>
                    <button string="Close" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_sale_order_mobile_wizard" model="ir.actions.act_window">
        <field name="name">Create/Open Sale Order</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">so.mobile.wizard</field>
        <field name="view_type">form</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="groups_id" eval="[(4,ref('sales_team.group_sale_salesman'))]"/>
    </record>

</odoo>
